# MongoDB (opcional — sem BD o servidor arranca mas endpoints de persistência devolvem 503)
MONGO_URL=mongodb://localhost:27017
DB_NAME=urbanflow

# CORS — origens permitidas (separadas por vírgula)
CORS_ORIGINS=http://localhost:3000
